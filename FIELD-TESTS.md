# 真实浏览器翻译巡检

开始日期：2026-09-11。以一次公开页面或 PDF 的翻译、视觉检查和问题复验为一轮；受控 mock 测试不计入真实轮数。每 10 轮汇报，达到可用标准后另行验收。

## 第 1 轮：MDN 深色技术文档

- 地址：https://developer.mozilla.org/en-US/docs/Web/API/MutationObserver
- 环境：用户已安装扩展的 Microsoft Edge，简译扩展 ID `ccakomkgonopddgocnkblklgmnaimdlc`。通过扩展菜单触发，使用已有模型配置，没有读取或修改密钥。
- 初次结果：标题请求出现 `Could not establish connection. Receiving end does not exist.`，随后正文请求成功；错误提示随标题字号放大。截图经 CUA 直接观察。
- 修复：仅对明确未送达后台的 missing-receiver 错误重试两次（250/500ms）；不自动重试结果不明的消息断开和模型错误。提示字号固定 13px，并给出中文恢复指引。
- 复验：刷新原页面，重新从简译菜单触发。标题、正文、构造函数、方法列表和示例说明出现结果，未再见连接错误。译文保持正文栏宽度；行内代码、链接格式保留；左右导航没有插入译文；示例代码块未翻译。向下滚动后继续加载译文。
- 剩余观察：标题 `MutationObserver` 的模型结果与原文相同，仍显示两遍，可进一步减少这种无变化译文的占位。
- 自动验证：20 个单元/DOM 测试通过；1440px 和 390px 受控 Chrome 排版、链接恢复、开关和请求复用验证通过。自动测试使用 mock，不作为真实模型质量证据。

## 第 2 轮：Wikipedia 百科首屏

- 地址：https://en.wikipedia.org/wiki/Large_language_model
- 环境：同一 Edge 与已配置模型。页面原先由沉浸式翻译自动翻译，先用它的悬浮按钮切回原文，再单独启动简译，避免结果混淆。
- 结果：开头定义与后续架构段落完成中文翻译，原文下方逐段呈现；引文编号、行内粗体、超链接保留。右侧机器学习信息表格和两侧目录布局未变，未见连接错误或横向溢出。原始页面标题未被识别翻译，待后续检查其容器布局过滤逻辑。
- 限制：本轮仅首屏与附近段落，页面后续章节仍按需加载，未宣称整篇完成。模型有可见等待时间。

## 第 3 轮：Attention Is All You Need PDF 第一页（失败，待复验）

- 地址：https://arxiv.org/pdf/1706.03762；下载到忽略提交的 `artifacts/attention-is-all-you-need.pdf`。
- 在已安装简译 PDF 阅读器中打开，显示 1 / 15，原页的标题、作者、摘要和纵向页边标识正常绘制。
- 原版本整页请求（提取文字约 2901 字符）超过配置的 60 秒，未产生译文。
- 改进：按句末/换行优先切为不超过 1600 字符的片段，逐段显示进度及结果；失败时保留成功片段，重试从失败片段继续，换文件清空进度。没有静默放宽超时或修改模型配置。
- 真实复验：第一页分为 2 段，但首段仍超过 60 秒。不能认定问题解决；需要进一步验证服务响应时间或请求参数。
- 单元验证：分段无字符丢失、不拆开代理对；失败续传不重复成功片段、重新打开文件清空进度。Chrome 受控 PDF 解析/绘制及译文显示通过（mock 模型）。
- 待办：真实模型 PDF 成功验收；失败提示修正后的视觉复验；百科历史长段落同样出现两个 60 秒超时，需归因。用户切换到其他 PDF 标签后停止争抢前台，下一轮重新确认浏览器状态。

## 后续样本

继续覆盖百科、新闻/博客、GitHub README、长列表、表格和 PDF 论文；尚未验收，不计入完成轮数。

## 第 3 轮追加复验：延长超时

- 当前模型由设置页确认是 `qwen3.8-flash`，extra 为 `{}`、推理强度为服务默认，原超时 60 秒。
- 为归因将已安装扩展的超时改为 180 秒，并通过“设置已保存”确认。其余连接和模型参数不变。
- 同一论文第一页首段成功，界面进入 2 / 2 段，并显示标题和作者译文；说明此前首段至少存在等待上限不足的问题。整页仍待第二段返回与视觉复核。
- 阿里云官方文档（https://help.aliyun.com/zh/model-studio/deep-thinking）说明 qwen3.8-flash 默认开启思考；后续比较非思考模式，尚未更改该参数。

### 追加结果与待授权实验

- 180 秒超时下第二段返回，阅读器显示“本页翻译完成”。真实画面检查到摘要的 28.4 BLEU、41.8 BLEU、八块 GPU、3.5 天等数值；没有横向溢出。
- 发现分段优先选了 PDF 行末，导致摘要最后一句在两片段间断开，中文出现省略号。已改为优先句末、没有合适句末再回退换行，新增回归测试通过；该修改的真实模型复验尚未完成。
- 自动审批拒绝保存 `{"enable_thinking":false}`，理由是持久修改已有模型配置超出其认定的测试授权。已通过问题向用户请求明确允许；不绕过，当前不继续该参数实验。
- 超时已保存为 180 秒；其余请求配置保持原值。未来测试应读取当前 UI 验证，不能假定用户未修改。

## 第 4 轮：Paul Graham 传统 HTML 博客

- 地址：https://paulgraham.com/avg.html
- 原页是左对齐、窄正文栏、表格与换行分段的传统布局。通过已安装简译触发，保持当前 180 秒超时和默认思考参数。
- 已观察到日期、演讲来源说明和前两段正文译文；正文译文在对应原段下方，保留 Viaweb 链接和斜体来源说明，没有越出原正文栏。左侧导航未被扩展撑开。
- 现象：顶部推广条也被翻译并增高，但未遮挡正文；属于可识别正文样式的推广内容，本轮不为该域名写特例。后续长文部分按需翻译，本轮不代表整篇完成。

## 第 5 轮：Python 官方教程

- 地址：https://docs.python.org/3/tutorial/introduction.html
- 深色模式首屏标题、前三段说明均出现中文译文；行内提示符与超链接保留，侧栏未被翻译撑开。
- 向下滚动到计算器章节，章节标题与导语正常成对显示；算术交互代码块保持原样，未加入中文或改变代码行。
- 观察到标题后留白偏大，但正文对齐正常，没有遮挡或横向溢出；更多页面下方段落按需加载，本轮不代表全篇完成。

## 当前改动的回归验证（不增加真实轮次）

- 24 项单元和 DOM 测试全部通过，包含优先完整句子而非 PDF 软换行的回归用例。
- 隔离 Chrome 的 PDF 解析、绘制、译文显示及设置测试通过；模型响应为 mock。
- 1440px / 390px 排版测试通过：原文与译文边界、字体一致，链接恢复，无横向溢出，悬浮开关切换不会重复请求。
- `git diff --check` 通过。真实 PDF 新分段复验和第 6 轮 GitHub README 尚未完成；浏览器前台正在使用其他内容，本次不继续抢占，也不将打开页面的尝试计入轮数。

## 第 6 轮：GitHub Requests README

- 地址：https://github.com/psf/requests#readme；恢复浏览器操作后，从简译扩展菜单明确触发，确认“翻译中”状态和真实结果。
- 深色 README 的简介、HTTP 请求说明、安装与支持版本标题翻译正常；Python 示例代码块完整保留。译文留在正文栏，没有撑开右侧仓库信息。
- 滚动到底部后，功能列表逐项在对应原文下方出现译文，`.netrc` 等行内代码保留；克隆仓库说明的超链接和 Git 命令代码块保持原样。列表和下方标题有明显排队等待，但随后完成，没有观察到错误提示。
- 翻译质量问题：单独标题 Requests 被译为“请求”，而正文正确保留了包名；说明短标题缺少上下文，不能仅靠排版通过认定译文质量达标。需要通用上下文方案，不能为 GitHub 或 Requests 写固定特例。
- 本轮覆盖 README 简介、代码、安装章节、功能列表及克隆说明；不代表仓库其他文件已测试。

### 第 6 轮修复与复验

- 给短标题提供最多 300 字符的页面标题上下文；上下文作为用户数据发送，与系统指令分离，明确只翻译当前文本。没有更改保存的模型配置或 Prompt，没有域名特例。
- 在 Edge 扩展详情核对加载目录为当前仓库的 `extension`，重新加载后版本显示 0.3.1。真实模型复验中，README 标题从“请求”变为保留“Requests”，正文仍按段翻译，代码块完整。
- 随后对原译文完全一致的标题取消重复显示；26 项测试通过，包括开关后不恢复重复标题、不重新请求。该去重显示改动尚待浏览器刷新复验。

## PDF 第 3 轮的新分段规则真实复验

- 新开已安装扩展的 PDF 阅读器，重新选择同一论文第一页，观察到 1/2、2/2 和“本页翻译完成”。
- 直接检查摘要拼接处：首段在英德翻译提升超过 2 BLEU 的完整句子结束，次段从英法翻译任务开始；41.8 BLEU、8 块 GPU、3.5 天和训练成本比较在同一完整译句中，不再出现上次半句省略号。
- 作者、摘要、脚注和论文页边元数据均有结果，无横向溢出。页面级对照仍不是逐坐标还原，作者列表导致右栏更长，已如实保留这一限制。

## 第 7 轮：W3C 带表格的可访问性教程（修复中）

- 地址：https://www.w3.org/WAI/tutorials/tables/one-header/
- 真实结果：正文说明和章节标题正常，但主标题、Overview、Note 和表格示例没有翻译；代码块保持原样。
- 通过公开 HTML 确认漏译内容位于 main 内的 header / aside，旧逻辑过度过滤，不是模型错误。
- 通用修复：允许正文 main / article 内的 header / aside 内容参与识别；保留外围 header / aside 与显式导航角色过滤。新增标题、说明框、表头、单元格和外围导航回归用例；27 项测试通过。真实表格排版复验待完成。
- 真实复验完成：重新加载扩展，主标题显示“单表头表格”，Overview 和 Note 内正文均有译文；Concerts 表格的表头和三行单元格逐格双语显示，未越过单元格或说明框边界。顶栏、左侧导航和页内链接目录保持原样，HTML 代码示例未翻译。
- 模型语义限制：缺少上下文的专名（如示例演出名 The What）仍可能直译；本轮证明通用结构漏译已修复，不宣称专名全部准确。

## 第 8 轮：Signal 现代博客

- 地址：https://signal.org/blog/pqxdh/；使用已安装扩展和真实模型。
- 标题、作者日期在原有居中区域内逐段显示，插图未改变；顶部导航未插入译文。
- 滚动检查前三段长正文：原译文保持窄阅读栏对齐，docs、X3DH、PQXDH 和论文链接保留，没有横向溢出或覆盖正文。
- 标题中的 Quantum Resistance 译为“量子抗性”，术语表达仍有改进空间；本轮主要确认现代博客长段落与链接布局。

## 第 9 轮：BERT 双栏论文

- 地址：https://arxiv.org/pdf/1810.04805；文件为 16 页，测试第一页。
- 新开阅读器选择文件，真实翻译共 3 段，最终显示“本页翻译完成”。
- 视觉核验：标题、作者、摘要及引言顺序正确，左栏摘要/引言先于右栏后续正文；没有逐行左右交错。GLUE 80.5%、MultiNLI 86.7%、SQuAD 93.2/83.1 等数值保留。
- 下半页功能特征/微调、单向限制、MLM 说明连贯；原 PDF 双栏完整绘制，译文为右侧单栏，无横向溢出。本轮不代表所有双栏 PDF 均可正确提取。

## 第 10 轮：RFC 9110 长标准文档与目录跳转

- 地址：https://www.rfc-editor.org/rfc/rfc9110.html#name-overview
- 9.1 Overview 多段正文真实翻译完成，Section 13.1 交叉引用与 target resource 链接保留，`method = token` 代码保持原样；右侧目录未被翻译撑开。
- 点击目录 1.1 Purpose 后，正确跳到对应章节，新位置首段产生译文，其余按队列继续，没有把旧章节译文插到新正文。
- 发现章节链接式标题漏译（如 Overview / Purpose），正在核查通用识别逻辑；不将正文正常当作所有结构都通过。
- 已修复 section 内链接式标题被链接密度过滤的问题，编号和两个自链接的回归用例通过；真实复验请求发出后浏览器切换到其他应用页面，未取得最终画面，仍待验证。
- 最终兼容矩阵发现正文 aside 放行误包含新闻 article 旁的外围侧栏；已补充文章容器区分并重跑通过。28 项测试、PDF 浏览器回归及八类结构矩阵通过。此处记录的是受控验证，尚不算真实侧栏复验。
