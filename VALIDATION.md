# 0.4.0 安装与首次使用验证

- 29 项单元 / DOM 测试通过，含设置页携带返回标签参数时的后台来源校验，其他页面不能发起连接测试。
- 隔离 Chrome 的首次使用流程通过：空配置引导、授权拒绝、已保存但测试失败、仅一个进行中的请求、成功后返回原页、原页已关闭、编辑后清除成功状态、折叠区域中的非法参数定位。
- 旧配置加载保持原有地址、模型、Prompt、超时与额外参数，不执行自动保存；没有读取或修改用户浏览器中的真实密钥。
- 1280px 首屏可见主要连接字段和测试按钮；390px 页面无横向溢出，截图已检查。
- 设置及 PDF 解析/绘制的原浏览器回归通过。新流程测试使用受控响应与权限模拟，不代表重新验收各服务商的真实接口或浏览器权限弹窗。
- 最终安装包解压后再次运行首次使用回归通过；归档完整性、manifest 版本、PDF 资源和源文件一致性检查通过。
- 安装包 SHA256：`ba8550996c8c5e2c6b9b56756602df0408921143c2c3d32c2bf56375fd884cf3`。

复现：`npm test`、`npm run test:onboarding`、`node tests/browser.mjs`。维护者使用 `npm run package` 打包（需要 Python 3）；最终用户直接下载 Release 附件。

---

# 0.3.2 真实巡检与回归验证

真实安装的 Edge 插件完成 10 轮样本检查，详细过程及限制见 [FIELD-TESTS.md](FIELD-TESTS.md)。覆盖 MDN、Wikipedia、传统博客、Python 文档、GitHub README、W3C 表格、Signal 博客、RFC 长文档和两篇 PDF。轮次包含失败与修复复验，不等同于 10 个全部通过的页面。

- 28 项单元 / DOM 测试通过。
- 隔离 Chrome 设置、模型请求（mock）、PDF 解析绘制和显示通过。
- 1440px / 390px 排版检查通过；新闻、文档、论坛、卡片、表格、自定义元素、隐藏内容与 600 段列表兼容矩阵通过。
- 真实验证通过：PDF 完整句子分段、BERT 双栏文字顺序、短标题页面上下文、正文 header / aside 的说明框和表格识别。
- 追加真实复验通过：RFC 链接式章节标题保留编号和链接，GitHub 相同标题去重。
- 侧栏追加真实复验通过：MDN 正文及构造函数说明产生译文，左右导航保持原样；配合新闻 article 旁 aside 的受控矩阵验证，未再发现本轮侧栏回归。
- 没有增加站点域名或专用类名规则，没有修改保存的模型 Prompt。模型专名直译、响应等待、复杂 PDF 提取顺序仍有限制。扫描件 OCR、iframe 和 Shadow DOM 不在当前实现范围。

升级方式：重新加载现有扩展，再刷新文章标签页；模型设置保留。此次版本号用于识别巡检修复集合。

本轮验收：已完成 10 轮真实样本检查及所发现渲染问题的追加复验。当前可作为精简版日常阅读基线，结束此次自动巡检；这不是对所有网站、PDF 或模型译文准确率的保证。模型专名处理及耗时仍取决于所配置服务。

---

# 0.3.0 通用网页翻译验证

日期：2026-09-11。范围：当前仓库的通用 DOM / CSS 识别与增量更新实现；没有站点域名或专用类名规则。

## 已完成

- `npm test`：18 项通过。
- `npm run test:browser`：设置页、PDF.js 解析/绘制、1440px / 390px 排版以及兼容矩阵全部通过。
- 原链接 DOM 和点击监听保留；译文链接仅从原文安全链接白名单恢复。
- 原地改字、子树替换、隐藏区域展开、节点追加、CSS inline/block 切换和 URL-only SPA 路由变化已验证。
- 迟到的旧请求不写入新内容；局部文字更新不重新扫描无关段落子树；插件自身插入不产生重复请求。

## 受控浏览器页面矩阵

| 场景 | 验证重点 | 结果 |
| --- | --- | --- |
| 新闻 Grid | 正文与侧栏分离，阅读栏宽度、原链接事件保留 | 通过 |
| 技术文档 | 标题、嵌套列表、定义列表、行内代码、代码块排除 | 通过 |
| 论坛 Flex | 头像/正文布局、可编辑草稿排除、节点复用、追加、切页 | 通过 |
| 卡片 Flex/Grid | 子项数量和列宽不变，匿名 Flex 文本和截断预览跳过 | 通过 |
| 表格 | 单元格内插入，纯数字跳过，列宽不变 | 通过 |
| 自定义元素与换行 | 块级 span/custom element、display:contents、inline DIV、嵌套 BR 分段 | 通过 |
| 过滤与展开 | ARIA 导航、隐藏内容、链接密集菜单排除，展开后识别 | 通过 |
| 600 段长列表 | 首次只请求视口附近 12 段，滚动到底部后请求末段 | 通过 |

以上均为真实 Chrome 中的受控 HTML/CSS 页面，使用 mock 模型响应。它们验证分段、请求范围、更新与渲染，不验证真实模型的翻译质量，也不能换算为真实网站兼容率。浏览器截图与 JSON 结果运行时输出到忽略提交的 `artifacts/`。

## 尚未覆盖

- 大规模公开站点抽样及真实模型在各类站点的端到端验收。
- iframe、Shadow DOM、canvas 文字、扫描件 OCR。
- 所有虚拟滚动与高度固定的阅读器实现；无法安全扩展的区域会跳过。
- 插件以真实安装身份的权限弹窗与 Service Worker 长时运行验收。

升级方式：重新加载现有扩展，再刷新文章标签页。保留原安装目录和模型配置。
