# 0.3.0 通用网页翻译验证

日期：2026-09-11。范围：当前仓库的通用 DOM / CSS 识别与增量更新实现；没有站点域名或专用类名规则。

## 已完成

- `npm test`：18 项通过。
- `npm run test:browser`：设置页、PDF.js 解析/绘制、1440px / 390px 排版以及兼容矩阵全部通过。
- 原链接 DOM 和点击监听保留；译文链接仅从原文安全链接白名单恢复。
- 原地改字、子树替换、隐藏区域展开、节点追加、CSS inline/block 切换和 URL-only SPA 路由变化已验证。
- 迟到的旧请求不写入新内容；局部文字更新不重新扫描无关段落子树；插件自身插入不产生重复请求。

## 受控浏览器页面矩阵

| 场景 | 验证重点 | 结果 |
| --- | --- | --- |
| 新闻 Grid | 正文与侧栏分离，阅读栏宽度、原链接事件保留 | 通过 |
| 技术文档 | 标题、嵌套列表、定义列表、行内代码、代码块排除 | 通过 |
| 论坛 Flex | 头像/正文布局、可编辑草稿排除、节点复用、追加、切页 | 通过 |
| 卡片 Flex/Grid | 子项数量和列宽不变，匿名 Flex 文本和截断预览跳过 | 通过 |
| 表格 | 单元格内插入，纯数字跳过，列宽不变 | 通过 |
| 自定义元素与换行 | 块级 span/custom element、display:contents、inline DIV、嵌套 BR 分段 | 通过 |
| 过滤与展开 | ARIA 导航、隐藏内容、链接密集菜单排除，展开后识别 | 通过 |
| 600 段长列表 | 首次只请求视口附近 12 段，滚动到底部后请求末段 | 通过 |

以上均为真实 Chrome 中的受控 HTML/CSS 页面，使用 mock 模型响应。它们验证分段、请求范围、更新与渲染，不验证真实模型的翻译质量，也不能换算为真实网站兼容率。浏览器截图与 JSON 结果运行时输出到忽略提交的 `artifacts/`。

## 尚未覆盖

- 大规模公开站点抽样及真实模型在各类站点的端到端验收。
- iframe、Shadow DOM、canvas 文字、扫描件 OCR。
- 所有虚拟滚动与高度固定的阅读器实现；无法安全扩展的区域会跳过。
- 插件以真实安装身份的权限弹窗与 Service Worker 长时运行验收。

升级方式：重新加载现有扩展，再刷新文章标签页。保留原安装目录和模型配置。
